{% extends 'base.html' %}
{% load static %}
{% load wt_extras %}

{% block title %}{{ block.super }} - GAS Follow Up Meeting Edit Goals{% endblock %}

{% block css %}
  {{ block.super }}
{% endblock %}

{% block script %}
{{ block.super }}
<script src="{% static 'jquery/jquery.js' %}"></script>
<script>
  $(function() {
    $( "#type" ).change(function() {
      $(".conditional-questions > div").hide();
      $(".conditional-questions #" + $(this).val()).show();
    });
    $("#type").prop('selectedIndex', -1);
  });
  function add_category() {
    var num = $("#cat-list > input").length + 1;
    $("#cat-list").append('<input type="text" id="cat' + num + '" name="cat' + num + '" class="form-control" placeholder="Category ' + num + '">');
    $("#cat-list > input").last().focus();
  }
</script>
{% endblock %}

{% block content %}
<h2>Follow Up Meeting - Edit Goals
  {% if user|is_physician %}
   - {% firstof patient.get_full_name patient.username|title %}
  {% endif %}
</h2>

<form class="form-horizontal col-md-6 col-md-offset-3" action="{% url 'followup_goaledit' patient.id %}" method="post">
{% csrf_token %}
    </br></br>      
   <div class="form-group">
     <label for="goal" class="col-sm-4 control-label">Current Selected Goal:</label>
     <div class="col-sm-8">
       <p class="help-block">{{ selected_goal.goal1 }}</p>
     </div>
   </div>

{% if banked_goals_list %}
          
    {% for gasgoal in banked_goals_list %}
   <div class="form-group">
     <label for="goal" class="col-sm-4 control-label">Banked Goal:</label>
     <div class="col-sm-8">
       <p class="help-block">{{ gasgoal.goal1 }}</p>
     </div>
   </div>

{% endfor %}
            
{% else %}
            <strong>There are no banked goals present.</strong><br/>
{% endif %}


</br>

<!-- User selection code -->

    <div class="form-group">
     <label for="goal" class="col-sm-5 control-label">What would you like to do?</label>
    </div>

           <div class="form-group">
             <label for="goal" class="col-sm-6 control-label">Keep selected Goal:</label>
	     <div class="col-sm-1">
	       <label class="radio">
	       <input type="radio" name="navselect" id="optionsRadios" value="keep" >
	     </label>
             </div>
	   </div>

           <div class="form-group">
             <label for="goal" class="col-sm-6 control-label">Edit selected goal:</label>
	     <div class="col-sm-1">
	       <label class="radio">
	       <input type="radio" name="navselect" id="optionsRadios" value="edit">
	     </label>
             </div>
	   </div>

           <div class="form-group">
             <label for="goal" class="col-sm-6 control-label">Select new goal from banked goals:</label>
	     <div class="col-sm-1">
	       <label class="radio">
	       <input type="radio" name="navselect" id="optionsRadios" value="select">
	     </label>
             </div>
	   </div>

           <div class="form-group">
             <label for="goal" class="col-sm-6 control-label">Create new goal:</label>
	     <div class="col-sm-1">
	       <label class="radio">
	       <input type="radio" name="navselect" id="optionsRadios" value="create">
	     </label>
             </div>
	   </div>

  {% if navselecterror == 'error' %}
      <div class="alert alert-danger">
        <strong>Error</strong> Please select an option above.
      </div>
  {% endif %}



  <div class="submit text-center">
    <input type="submit" class="btn btn-lg btn-success"></input>
  </div>
</form>
{% endblock %}
